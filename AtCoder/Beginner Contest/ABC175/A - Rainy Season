#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);cout.tie(0);
    string s;
    cin>>s;
    cout<<(s[1]=='R'?count(s.begin(),s.end(),'R'): count(s.begin(),s.end(),'R')==0?0:1);
    return 0;
}
